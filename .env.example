# ============================================================
# Pastor Fabiano Gonçalves — Variáveis de Ambiente
# Copie para .env e preencha os valores
# NUNCA commitar o .env no GitHub
# ============================================================

# ── Secrets do Worker (configurar via wrangler secret put) ──
# ADMIN_TOKEN=gere-com: openssl rand -hex 32
# TELEGRAM_BOT_TOKEN=8954357731:AAFv...  (token do @IAprFabianogoncalves_bot)
# TELEGRAM_GROUP_ID=-100XXXXXXXXXX       (ID negativo do grupo de aprovação)

# ── Dashboard local (opcional) ──────────────────────────────
# URL da API em desenvolvimento:
VITE_API_URL=http://localhost:8787

# ── Cloudflare Pages ─────────────────────────────────────────
# Configurar em: Cloudflare Pages → Settings → Environment variables
# VITE_API_URL=https://fabiano-api.workers.dev
